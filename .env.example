# Environment variables template
# Copy to .env and fill in values (never commit .env)

# ATAC_BASE=/path/to/project/root
# ATAC_RAW_FASTQ=/path/to/raw/fastq/files
# ATAC_REFERENCE_DIR=/path/to/reference/files
